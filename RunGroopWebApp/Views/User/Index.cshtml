﻿@using RunGroopWebApp.ViewModels
@model List<UserViewModel>

<section class="flush-with-above">
    <div class="container">
        <div class="row">
            <div class="col">
                <table class="table table-hover align-items-center table-borderless">
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr class="bg-white">
                                <th scope="row">
                                    <div class="media align-items-center">
                                        <div class="media-body">
                                            <span class="h6 mb-0">@item.UserName</span>
                                        </div>
                                    </div>
                                </th>
                                <td>
                                    <a class="btn btn-outline-primary" asp-controller="User" asp-action="Detail" asp-route-id="@item.Id">
                                        View Profile
                                    </a>
                                </td>

                                @{
                                    var mileage = item.Milage == null ? "?" : item.Milage.ToString();
                                    var pace = item.Pace == null ? "?" : item.Milage.ToString();
                                }
                                <td>@mileage mile at @pace mph</td>
                                <td><i class="icon-location"></i> @item.Milage</td>
                            </tr>
                            <tr class="table-divider">
                                <th></th>
                                <td></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <!--end of col-->
        </div>
        <!--end of row-->
    </div>
    <!--end of container-->
</section>